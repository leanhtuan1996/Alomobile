'use strict';
var dashboard = require('./dashboard');
var product = require('./product');
var category = require('./category');

module.exports = {
    dashboard: dashboard,
    product: product,
    category: category
}